import 'package:flutter/material.dart';

// Colors
const Color primaryBG =
    Color.fromARGB(255, 0, 24, 54); // Primary background color
const Color addItemField =
    Color.fromRGBO(0, 105, 92, 1); // Add item field color
const Color appbar = Color.fromARGB(255, 14, 53, 100); // App bar color
const Color appbarText = Color.fromRGBO(187, 222, 251, 1); // App bar text color
const Color primaryBtnText =
    Color.fromARGB(255, 13, 96, 163); // Primary button text color
const Color primaryBtnBG =
    Color.fromARGB(255, 18, 116, 196); // Primary button background color
const Color pageBG =
    Color.fromARGB(255, 206, 238, 253); // Page background color

// API Key
const String rapidApiKey =
    "0ddcb48f11msh7f0b44853126888p15f717jsn132453386804"; // RapidAPI key

// Default Image
const String defaultImage =
    "assets/images/image-placeholder.png"; // Default image path

// Notification Channels and IDs
const String feItemChannelId =
    "feItem"; // Food Eye Item notification channel ID
const String feItemChannelName =
    "Food Eye Item Notifications"; // Food Eye Item notification channel name
const int feItemNotificationId = 0; // Food Eye Item notification ID
const String dateCheckerChannelId =
    "dateChecker"; // Date Checker notification channel ID
const String dateCheckerChannelName =
    "Date Checker Notifications"; // Date Checker notification channel name
const int dateCheckerNotificationId = 1; // Date Checker notification ID

// Duration
const hundredYears = Duration(days: 365 * 100); // Hundred years duration
